function validateControllerConfig(controllerConfig) {
  if (!controllerConfig.controlEmjois) {
    console.warn(
      'No control emojis specified in controllerConfig',
      controllerConfig
    );
  }
  if (!controllerConfig.) {

  }
}